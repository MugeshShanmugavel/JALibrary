export class Book{
    isbn:string;
    name:string;
    transactId:string;
    borrowDate:string;
    returnDate:string;
    constructor(isbn:string, name:string, tId:string, bDate:string, rDate:string){}
}